import { createStyles } from '@material-ui/core/styles'

const styles = createStyles({
    link: {
      textDecoration: 'none'
    },
    menuButton: {
        marginLeft: -12,
        marginRight: 20
    },
    drawer: {
        minWidth: 250,
        width: '100%'
    }
})

export default styles
