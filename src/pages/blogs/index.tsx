import * as React from 'react'
import Header from '../../components/header'

const BlogsPage = () => {
    return <section>
        <Header>
            Blogs
        </Header>
    </section>
}

export default BlogsPage
